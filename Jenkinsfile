pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Quiz App Build Started'
                echo 'Quiz App Build Successful'
            }
        }
    }
}